[package]
name = "ulv-core"
version = "1.3.0"
edition = "2021"
license = "AGPL-3.0-or-later"
publish = false

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
insta = { version = "1", features = ["yaml"] }
